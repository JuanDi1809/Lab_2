/**
* @brief Crea una matriz usando memoria dinamica 
* @param tam Es el tamaño que debe tner la matriz
* @return Un doble puntero que apunta al arreglo de punteros que apuntan a las filas de la matriz 
*/
unsigned int** crearMatriz(unsigned short int tam);

/**
* @brief Libera la matriz creada con la memoria dinamica 
* @param matriz Un doble puntero que apunta al arreglo de punteros que apuntan a las filas de la matriz 
* @param tam es el tamaño de la matriz
* @return No retorna nada 
*/
void liberarMatriz(int **matriz, short int tam)
